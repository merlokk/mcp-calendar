[project]
name = "mcp-calendar"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.1.0",
    "icalendar>=5.0.0",
    "pytest>=9.0.2",
    "pytz>=2024.1",
    "recurring-ical-events>=3.0.0",
]
